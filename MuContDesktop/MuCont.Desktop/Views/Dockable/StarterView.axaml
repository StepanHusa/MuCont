<UserControl
    x:Class="MuCont.Desktop.Views.Dockable.StarterView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MuCont.Desktop.ViewModels.Dockable"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:DataType="vm:StarterViewModel"
    mc:Ignorable="d">
    <Grid>

        <!--<StackPanel>
            <TextBlock Text="Initial Point" />
            <ItemsControl Items="{Binding Coordinates}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Margin="4"
                            Orientation="Horizontal">
                            <TextBlock
                                Width="100"
                                Text="{Binding Name}" />
                            <TextBox
                                Width="100"
                                Text="{Binding Value, Mode=TwoWay}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Items="{Binding Parameters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Margin="4"
                            Orientation="Horizontal">
                            <TextBlock
                                Width="100"
                                Text="{Binding Name}" />
                            <TextBox
                                Width="100"
                                Text="{Binding Value, Mode=TwoWay}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


            <TextBlock Text="Monitored singularities" />
            <StackPanel />
            <TextBlock Text="eigenvalues" />
        </StackPanel>-->
    </Grid>
</UserControl>
