Lets get back to the api idea. I store the info to ode system in a file on the server. I have the (REST) api written in julia on the server and it should call libraries (also in julia) for computation. I need to run jobs and keep the api responsive. i need to keep track of what jobs aare running and be able to stop the computation, also during the computation, computed data will be continously created and the api should make them visible.  