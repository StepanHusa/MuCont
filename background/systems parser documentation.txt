- The system is in json, that should be clear.
- Id must be unique and it should be possible to make a filename from it.
- Variables ar distinct strings, that must be parsable by the Symbolic library.
- functions must be in the correct order if referencing one another.
- equations can make use of functions, they should be in the order of coordinates, but the program might be able to correct. 
- the display functions should be independent. (I can change it later)

- supports fuctions exp,ln,sin,cos,sqrt,other are probably might work as well, nonsmooth will make problems if evaluated at the point where ithe does not have derivative 